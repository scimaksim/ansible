---
  - name: Create a VLAN
    hosts: cisco
    connection: network_cli
    become: yes

  tasks:
    - name: Create a VLAN
      net_vlan:
        vlan_id: "{{item}}"
        state: present
      loop: "{{vlan_id}}"
