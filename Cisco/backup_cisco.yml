---
- hosts: cisco
  connection: network_cli
  remote_user: david
  become: yes
  become_method: enable
  ansible_ssh_password: cisco

  tasks:
    - name: Backup configuration
      ios_config:
        backup: yes
